# L4-03 — Multi-repository evidence audit, producer r2

2026-09-13. This bounded source-checkout exercise uses deep-research contract 0.1 and explicitly selected open-source-ecosystem 0.1.0/core 0.1. The actual newly written pack is available; its designed status does not establish installation, procedure demonstration or behavioural acceptance. Checkout HEAD observed b9d262b4bd2ffa73d35b520f98629e7eae0b7da7; the working-tree pack revision is its manifest version, not a claim that its new files exist in that commit. Parent owns the separate research-evaluate audit.

## Assessment

The twelve repositories form a heterogeneous evidence-processing ecosystem, not twelve interchangeable research engines. The decision is to keep roles and evidence origins explicit: orchestrators may request retrieval; parsing and browser tools return source representations; repositories/databases supply or transform data; evaluation frameworks require valid task-specific tests. A complete workflow must still carry original source identity and reviewed claims between components.

Two consequential cross-source claims were actually audited in claim-audit.md. Shared Playwright dependency declarations substantiate a common browser engine beneath two interfaces, so these projects cannot be counted as independent reliability evidence. The universal claim that local research/evaluation tools keep all data local is contradicted by hosted model/search configuration and Promptfoo's provider request path. A locally running harness and a locally processed workload are different propositions.

A natural source-locator defect was found and repaired: GROBID README.md returned 404 at its pinned SHA. Same-ref root discovery found Readme.md; only that documentary support chain was repaired. The original failure and provisional claim state remain preserved in provisional/. No synthetic mutation was necessary.

## Source strategy, resource accounting and limits

The specified repository list was the complete candidate set: no popularity exclusions or additional candidate repositories. Each first-pass acquisition attempted one latest commit metadata record, one pinned README and one consequential code/configuration file. GitHub connector was the native source route. No searches were invented: **0/20 searches**. The 36 first-pass source actions included one failed README locator; 23 file representations and 12 metadata records succeeded. Metadata resolution is recorded separately from selected documentary/code artefacts.

Targeted follow-up added three source actions: GROBID root listing at the same SHA, Promptfoo OpenAI chat provider file and corrected GROBID Readme.md. Total **39/64 source actions**, including the failed action; 25 remain, with 16 reserved for parent review and nine producer-available. Selected file artefacts overall: 25 successfully read (24 baseline roles plus one provider follow-up), one failed file-path attempt; one scoped directory representation. This is below the 36 selected documentation/code artefact ceiling. Commit metadata was acquired for pinning and is not counted as an additional selected capability artefact. No pagination/history continuation, paid calls, installations, purchases, credentials, external communications or engine execution. No outstanding requests. Local reuse of already acquired text and output authoring are not new source acquisitions.

Source retrieval used full UTF-8 file representations; analysis selected the exact functions/fields cited in ecosystem-map.md and claim-audit.md rather than implying an exhaustive code audit. README images, linked benchmarks and outside documentation were not followed. The top-level source record captures URLs, refs, content, access and the actual failed locator. Commit timestamps establish inspected state only; no currentness beyond these pins, maintenance ranking or vulnerability certificate follows.

## Producer completion and independent review

Producer self-check: twelve exact identities pinned; one documentation plus one code/configuration observation per repository after repair; two cross-source claims adjudicated; C2 targeted provider follow-up; D1 original failure preserved; other eleven repository records preserved. The attached change-record.md identifies affected rechecks and remaining limits. The resulting assessment is a completed bounded producer draft, awaiting fixed-input independent evaluation in evaluation/audit.md. No independent verdict is fabricated here.

