# L3-03 — Tests still required, producer r1

**None of these tests was executed.** This task authorises static inspection only. A later owner must authorise installation, any provider use and a finite spend/network boundary before testing.

| Gate | Discriminating test | Required observation |
|---|---|---|
| Installation and resolution | Build each pinned engine in an isolated environment at its declared minimum and README example Python versions, with a recorded dependency lock and platform. Include STORM's pinned DSPy and unpinned scientific/provider dependencies. | Actual command outcomes, resolved versions and no silent substitute revision; incompatible matrix cells remain ineligible. |
| GPT Researcher licensing | Reconcile Apache LICENSE/README with MIT pyproject metadata through authoritative clarification or qualified review. | A recorded applicable rights decision; no assumption that package metadata overrides the licence file. |
| Source policy | Provide two permitted sources plus a disallowed redirect, off-domain link and injected instruction; exercise each selected retriever/MCP/scraper path with outbound requests logged and denied by policy. | Zero disallowed source fetches; skipped work and attempted violations visible. URL selection alone is insufficient. |
| STORM exclusions | Insert an excluded URL into a VectorRM collection and compare to an audited search retriever or policy wrapper. | Explicit filtering before use. The inspected VectorRM exclude_urls argument is ineffective, so do not rely on it. |
| Local/private processing | Use synthetic private markers, pre-provision selected local model/embedding assets, block and log outbound DNS/HTTP, disable optional hosted tracing, exercise parsing/retrieval/reporting and error paths. | Document where data/logs persist and demonstrate the intended boundary; any hosted model/embedding/search path makes an all-local assertion fail. |
| Aggregate exposure | Use deterministic stubs with worst-case query branching, retries and concurrency; impose a controller deadline, request cap and token/spend cap before any paid trial. | Attempted/completed counts including retries and child tasks; limits stop additional dispatch and reconcile in-flight work. |
| ODR failure visibility | Force one research child to raise a non-token exception at deep_researcher.py 332–342, then inspect graph end state and final adapter status. | Partial work cannot be accepted as complete; preserve the error and completed evidence. No failure rate is inferred from this static branch. |
| GPT Researcher failure | Make one retriever time out or raise while another succeeds; inspect gathered exceptions, event stream, sources, costs and report status. | Partial failure is accountable rather than silently disappearing from the output audit. |
| STORM interrupted phase | Terminate between research/outline/article phases and mid-thread retrieval; restart using only complete persisted phase outputs. | Missing or partial files are detected, children terminate, accepted earlier evidence remains unchanged. |
| Cancellation and billing | Cancel with requests in flight, simulate rate limits and lost responses; test resume without blind retries. | Terminal/partial/cancelled state, outstanding request liability and measured billing if later authorised. |
| Evidence intake | Audit every consequential report claim from a small fixed corpus, including contradictory and duplicated sources. | Original URL/ref/passage, actual access, transformations, independent-origin assessment, unsupported claims and omissions available to a separate evaluator. |
| Output handoff | Validate a completed, failed and cancelled job against the consumer evidence contract. | No automatic acceptance of generated citations, no promotion of source text into instructions, no downstream deployment or publication. |

Reopen only affected claims when an engine ref, retriever, model/provider, dependency resolution, data boundary or required output contract changes. Preserve this static assessment for its original refs.

