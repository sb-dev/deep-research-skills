# Bounded engine assessment — core only, revision 1

`langchain-ai/open_deep_research` at `1b7d2e80db9faa586165c60e09096dbbfd483a64` is a plausible candidate for optional research execution, based on its documented configurability and statically visible research graph. These sources do not establish its fitness as an evidence-led skill's acceptance or review layer. Retain it as a candidate for further verification; this report neither approves deployment nor establishes tested reliability.

## Brief, method and actual execution

**A01–A02.** Assess capabilities, setup, licence scope, material limitations and the next verification step for an evidence-led research skill. Historical validity is restricted to the named engine commit, not current `main`. The authorised universe is README.md, LICENSE, pyproject.toml, langgraph.json, configuration.py, deep_researcher.py and Git commit metadata. Only original-source documentary reading and static inspection were permitted. No installation, engine execution, tests, code changes, paid operations, maintainer contact, security inference from popularity or deployment decision was authorised or performed.

The producer used the source-checkout deep-research skill at `/workspace/scratch/b99193148918/deep-research-skills`; `git rev-parse HEAD` returned `b9d262b4bd2ffa73d35b520f98629e7eae0b7da7`. Read `skills/deep-research/SKILL.md`, the common command contract, frame/plan/discover/extract-evidence/analyse-evidence/synthesise commands, and evidence-contract/source-and-execution/effort-and-repair references. No Extension Pack, historical evaluation, repair record or other benchmark report was read. This was actual execution of the skill's responsibilities using authorised host tools, not execution of the engine or a script named after a command. No subdelegation occurred within this case. Runtime observed: Codex Work Mode, JavaScript tool orchestration and bash local file tools. Exact underlying model/version was not available and is not inferred.

Before collection, the source-action ceiling was eight: seven reserved for named originals and one available for a necessary recovery; no engine/API-test budget. Finalisation used local synthesis and report review with no further source acquisition. Discovered the GitHub plugin's `github_fetch`, `github_fetch_file` and `github_fetch_commit` contracts; selected existing `mcp__codex_apps__github_fetch` with exact immutable URLs. Git-data commit metadata avoided a broader commit diff. No search, browsing of linked destinations or additional source discovery was performed.

## Actual source and access register

All seven acquisitions succeeded on 2026-09-13. Each file was returned as original UTF-8 text by `github_fetch`; all six file texts were inspected. The metadata response was JSON. All are included, primary for their own declarations/code, and share one repository origin: they are not seven independent validations. External claims in README remain project assertions. Retrieval date does not advance the commit's validity date.

| Action / ID | Exact original | Actual access and useful locator |
|---|---|---|
| 1 / S1 | [Git commit metadata](https://api.github.com/repos/langchain-ai/open_deep_research/git/commits/1b7d2e80db9faa586165c60e09096dbbfd483a64) | JSON fields `sha`, `author`, `committer`, `tree`, `message`, `verification`; no parent, tree or message links followed. |
| 2 / S2 | [README.md](https://github.com/langchain-ai/open_deep_research/blob/1b7d2e80db9faa586165c60e09096dbbfd483a64/README.md) | 10,233 returned characters; Quickstart, Configurations, Evaluation, Deployments and Usage, Legacy Implementations. Images were not retrieved or inspected. |
| 3 / S3 | [LICENSE](https://github.com/langchain-ai/open_deep_research/blob/1b7d2e80db9faa586165c60e09096dbbfd483a64/LICENSE) | 1,065 characters; complete MIT grant, notice condition and disclaimer. |
| 4 / S4 | [pyproject.toml](https://github.com/langchain-ai/open_deep_research/blob/1b7d2e80db9faa586165c60e09096dbbfd483a64/pyproject.toml) | 2,020 characters; `[project]`, dependencies, build and package configuration. |
| 5 / S5 | [langgraph.json](https://github.com/langchain-ai/open_deep_research/blob/1b7d2e80db9faa586165c60e09096dbbfd483a64/langgraph.json) | 295 characters; graph, Python, environment, dependency and auth entries. |
| 6 / S6 | [configuration.py](https://github.com/langchain-ai/open_deep_research/blob/1b7d2e80db9faa586165c60e09096dbbfd483a64/src/open_deep_research/configuration.py) | 8,234 characters, 252 lines; SearchAPI, MCPConfig, Configuration and from_runnable_config. |
| 7 / S7 | [deep_researcher.py](https://github.com/langchain-ai/open_deep_research/blob/1b7d2e80db9faa586165c60e09096dbbfd483a64/src/open_deep_research/deep_researcher.py) | 30,117 characters, 719 lines; complete returned module inspected, including error and graph construction paths. |

Actions 2–7 were submitted concurrently; their numbers identify requests, not claimed completion order. Long combined tool display was truncated. Cached source responses were retained in session memory and re-presented for complete inspection: S6 lines 1–252 once; S7 lines 1–305 and 306–719 in two non-overlapping displays. These are disclosed cache rereads, not remote reacquisitions. Local skill reference output was also reread where truncated. Source transformations were UTF-8 text presentation, in-memory line numbering and paraphrase/static reasoning; no OCR or source modification. Original source bytes were not independently hash-verified. The report preserves immutable locators and bounded extracts; session-memory caches are not a durable source archive.

**Budget at stop:** seven of eight source actions consumed; one unused; zero outstanding reservations/jobs. Metadata counts as one and every file as one. Cache rereads and local skill/report reads are disclosed separately. There were zero searches and zero engine runs.

## Evidence, claims and assessment

| Claim | Evidence and support state | Implication / qualification |
|---|---|---|
| C1 — The pinned revision is identifiable. | S1 reports the requested SHA, tree `3bf2795b06fbc76447511365df3b2c02743019b7`, and author/committer date `2026-08-10T18:13:29Z`. Its message describes an h2 4.3.0 → 4.4.1 dependency bump. **Supported metadata observation.** | The message is not inspection of the actual dependency resolution or lockfile. GitHub's reported signature verification does not establish code security. |
| C2 — The design can clarify, form a brief, delegate concurrent research, compress findings and generate a report. | S7 `clarify_with_user` lines 60–115; `write_research_brief` 118–175; `supervisor_tools` 225–349; `compress_research` 511–585; main graph 699–719. **Supported static observation; operational capability untested.** | These stages could supply execution results to a skill. Their presence does not demonstrate evidential sufficiency or successful operation. Clarification can be disabled; when needed it returns an end command with a question. |
| C3 — Provider and tool choices are configurable. | S2 Configurations documents multiple LLM providers, Tavily by default, OpenAI/Anthropic native search and MCP, with structured-output and tool-calling model requirements. S6 SearchAPI lists anthropic/openai/tavily/none; MCPConfig exposes URL/tools/auth_required; model fields separate summarisation, research, compression and writing. S7 uses `init_chat_model`, `get_all_tools` and configurable models. **Documentation plus static support, qualified.** | The README's broad MCP/provider compatibility is not an interoperability test. Tool implementation and prompts are imported from files outside the allowed universe. Default models are gpt-4.1-mini for summarisation and gpt-4.1 for the other three roles; these are engine configuration values, not the producer's actual model. |
| C4 — Setup declarations differ in scope, not necessarily in truth. | S4 declares package version 0.0.16 and `requires-python = ">=3.10"`. S5 selects `"python_version": "3.11"`; S2 Quickstart selects `--python 3.11`. **Supported documentary/static distinction.** | Python ≥3.10 is package metadata; 3.11 is the selected server/example version. Neither successful Python 3.10 compatibility nor incompatibility is demonstrated. |
| C5 — Several bounds exist, but the inspected configuration is not a global cost cap. | S6 defaults: concurrency 5, supervisor iterations 6, researcher tool-call iterations 10, structured-output attempts 3. S7 slices delegated calls (291–292), gathers them (305), and returns overflow errors (315–321). Supervisor comparison is `>` (247), and researcher checks its iteration limit after executing a batch (473–492). **Supported static observations; cost implication is inference.** | A tool-call iteration can contain multiple calls; the supervisor threshold is not simply an inclusive six-call promise. UI min/max metadata is not itself demonstrated runtime enforcement. No total monetary budget is established by these fields. |
| C6 — A broad exception can terminate the supervisor research phase. | S7 lines 288–342 wrap delegated execution and result processing in `except Exception as e`; line 334 evaluates `is_token_limit_exceeded(e, configurable.research_model) or True`, then returns `goto=END` with existing notes/brief. Main graph line 715 connects research supervisor to final report generation. **Supported static control-flow finding.** | If the error classifier returns normally, its boolean result cannot prevent this branch, including for a non-token error. The handler itself could raise; this is not a claim that every exception everywhere is swallowed. The handler does not attach the caught exception to its returned update. Inference: research failure may lead to writing from partial prior notes without a distinct failure state in that update. No observed run establishes frequency, output appearance or orchestration effects. |
| C7 — Textual notes and model summaries do not alone establish an evidence contract. | S7 compression returns raw notes assembled from tool/AI message content (553–585); errors can become strings (427–432); compression may remove older messages (568–570); final writing can truncate findings (661–683) and returns an override clearing notes (621–623, 654–659). **Static observations; integration judgement qualified.** | Useful material is exposed, but completeness, original-access depth, stable locators, contradiction tracking and source restrictions are not verified end to end. Prompt/state/tool implementations were not inspected. This report does not assert their universal absence from the repository. |

## Setup and licence scope

The README's documented local sequence is cloning, creating/activating a virtual environment, installing dependencies with uv, copying `.env.example` to `.env`, then launching `langgraph dev --allow-blocking` through `uvx` with Python 3.11. Studio accepts a question in `messages` and offers assistant configuration. The manifest maps “Deep Researcher” to `deep_researcher.py:deep_researcher`, loads `.env`, depends on the local package and references `./src/security/auth.py:auth`. These are instructions and declarations, not successful setup evidence. The environment example and auth implementation were outside scope; required key names and authentication adequacy therefore remain unverified. Provider credentials/services would need configuration. S4 lists numerous dependencies, often lower-bounded rather than exactly pinned; the allowed evidence does not establish a reproducible resolved environment. See S2 Quickstart/Configurations, S4 and S5; C3–C4 qualify compatibility.

S3 grants MIT permissions over the software and associated documentation, including use, modification, distribution and sale, conditional on retaining copyright and permission notices in copies or substantial portions. It disclaims warranties and liability. S4 also labels the package MIT. This is a documentary summary of the repository licence, not specialist legal clearance. It does not establish licence terms for every dependency, model weight, hosted API, search service, MCP server, dataset or retrieved source; those terms were not examined. “Open source” does not establish free external execution or rights in retrieved material.

## Limitations and next verification step

The README's performance/leaderboard claims and evaluation-cost figures are historical project reports associated with other listed commits and model configurations; their underlying evaluations were not accessed or reproduced. They do not establish this revision's accuracy, comparative performance, expected workload cost or security. The named originals directly support configuration and code declarations, with limited independent corroboration of operational claims.

For an evidence-led skill, the integration assumption behind candidacy is that the skill retains authority over the brief, permitted sources, spending, evidence intake, claim support, uncertainty and final acceptance. C2–C3 support investigating an execution adapter; C5–C7 make unconditional adoption unsupported. Imported utility/prompt/state/auth code, dependency resolution, provider compatibility, resource exposure, data handling and output provenance remain open. This bounded task accepts those as limitations rather than resolving them by leaving the authorised universe.

**Next verification step, proposed only:** define a separately authorised, isolated fixture-based harness for this exact revision, with mocked model/tool calls and no external services. Its first discriminating case should inject a non-token exception into a delegated research task and examine the actual returned state and subsequent writer input: can a consumer distinguish partial/error results from completed research and preserve available evidence? Include a normal completion fixture for comparison. Before execution, explicitly authorise any needed dependency installation or additional source inspection. Reconcile the declared Python ≥3.10 range with the selected 3.11 environment through a separately authorised compatibility check. No harness was written or run here; no tests passed or failed. An adapter decision remains with the consuming project and depends on those results.

## Producer self-check and delivery

**A11, target: this report revision 1.** The same producer checked the exact report wording against S1–S7 and the request. Coverage includes capabilities, setup, licence boundaries, Python declaration versus selected version, broad exception-to-termination path, limitations and a concrete next verification step. Material findings retain documentary/static/inference labels and unexecuted-test qualifications. The exception claim is scoped to its actual try/except and includes the classifier-return qualification; the Python distinction does not become an invented incompatibility. README performance is attributed, not adopted. Source identity, shared origin, cache rereads, seven-action budget and excluded sources are explicit. No runtime, security, specialist licence or deployment assurance is claimed.

This is a producer self-check, not an independent verdict, human expert review or benchmark score. No separate evaluator was used or required for this bounded production case. Outcome: the bounded documentary/static question is answered, with operational fitness still unverified. Report delivery is the separate file `benchmarks/progressive/packs/oss/rerun/core.md` in the authorised checkout; no external publication occurred. Changes to the target revision, proposed adapter, permitted sources/providers or deployment requirements would trigger renewed assessment of the affected claims.
